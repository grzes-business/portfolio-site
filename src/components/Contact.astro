---
---

<!-- <section id="contact" class="py-20 px-4" x-data="{ isOpen: false }">
  <div class="max-w-6xl mx-auto text-center">
    <h2 class="text-4xl font-bold mb-8">Let's Work Together</h2>
    <button
      @click="isOpen = true"
      class="inline-block px-8 py-4 bg-gradient-to-r from-blue-500 to-purple-500 rounded-full text-white font-medium hover:opacity-90 transition-opacity"
    >
      Get in Touch
    </button>
  </div>

--><!-- Modal --><!--
  <div
    x-show="isOpen"
    x-transition
    class="fixed inset-0 bg-black/50 backdrop-blur-sm flex items-center justify-center p-4 z-50"
  >
    <div
      @click.away="isOpen = false"
      class="bg-zinc-900 rounded-xl p-8 max-w-md w-full"
    >
      <h3 class="text-2xl font-bold mb-6">Contact Me</h3>
      <form class="space-y-4">
        <div>
          <label class="block text-sm font-medium mb-2">Name</label>
          <input type="text" class="w-full px-4 py-2 rounded-lg bg-zinc-800 border border-zinc-700 focus:outline-none focus:border-blue-500" />
        </div>
        <div>
          <label class="block text-sm font-medium mb-2">Email</label>
          <input type="email" class="w-full px-4 py-2 rounded-lg bg-zinc-800 border border-zinc-700 focus:outline-none focus:border-blue-500" />
        </div>
        <div>
          <label class="block text-sm font-medium mb-2">Message</label>
          <textarea rows="4" class="w-full px-4 py-2 rounded-lg bg-zinc-800 border border-zinc-700 focus:outline-none focus:border-blue-500"></textarea>
        </div>
        <button type="submit" class="w-full py-3 bg-gradient-to-r from-blue-500 to-purple-500 rounded-lg font-medium">
          Send Message
        </button>
      </form>
    </div>
  </div>
</section> -->